<main class="relative w-full h-full flex flex-col items-center">
    <div class="fixed inset-0 -z-10 overflow-hidden pointer-events-none">
        <video class="w-full h-full object-cover" loop autoplay playsinline disablepictureinpicture muted>
            <source src="../../src/assets/video/background.mp4" type="video/mp4">
        </video>

        <div class="absolute inset-0 bg-black/97.5" aria-hidden="true"></div>
    </div>

    <slot />
</main>